# The dsh-whale-particles-bg bundle layer: one row, so installing this package
# is all it takes to compose the plugin (no hand-edited profile patch needed).
# The row id is the loader identity; the package name is what Node resolves.
- insert:
    - id: whale-particles-bg
      name: dsh-whale-particles-bg
